<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Solange's Website</title>
	</head>
	<body>
		<header class="site-header">
			<a class="site-name" href="/">MSB &amp; Partners</a>
			<nav class="site-nav">
				<a href="/">Home</a>
				<a href="/shop">Shop</a>
			</nav>
		</header>
		<div id="page">
			<slot />
		</div>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}

	body {
		display: flex;
		flex-direction: column;
	}

	.site-header {
		position: sticky;
		top: 0;
		z-index: 50;
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 12px 20px;
		background: rgba(255, 255, 255, 0.7);
		backdrop-filter: blur(50px);
		border-bottom: 1px solid rgba(17, 24, 39, 0.08);
		font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
	}

	.site-name {
		font-weight: 600;
		font-size: 16px;
		color: #111827;
		text-decoration: none;
		letter-spacing: -0.01em;
	}

	.site-nav {
		display: flex;
		gap: 18px;
	}

	.site-nav a {
		color: #4b5563;
		text-decoration: none;
		font-size: 15px;
		transition: color 0.2s;
	}

	.site-nav a:hover {
		color: #111827;
	}

	#page {
		flex: 1 1 auto;
		display: flex;
		flex-direction: column;
		min-height: 0;
	}

	@media (max-width: 480px) {
		.site-header {
			padding: 10px 16px;
		}
	}
</style>
